<h2> <%= @recipe.title %></h2>

<h3>Ingredients</h3>

<ul>
<% @recipe.ingredients.each do |ingredient| %>
  <li><%= link_to "#{ingredient.name} X #{ingredient.quantity}", ingredient_path(ingredient) %></li>
<% end %>
</ul>
